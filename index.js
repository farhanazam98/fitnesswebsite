var button;
var input;

function preload() {}

function setup() {
	// createCanvas(windowWidth/4, windowHeight/4);
	// button = createButton"(Press Me");
	// button.mousePressed(exercise);
	// nameP = createP('Your name');
	//
	// slider = createSlider(10, 100, 86);
	// input = createInput("Enter Weight");
	// nameP.mouseOver(printAction);
	// nameP.mouseOut(mouseNotOver);
	// input.changed(updateText);
}

// function updateText(){
// 	nameP.html(input.value());
// }
// function printAction(){
// 	 nameP.html('your mouse is over me');
// }
//
// function mouseNotOver(){
// 	nameP.html('mouse is not over');
// }
//
// function exercise(){
// 	createP("I like to exercise");
//
// }

function draw() {
	// background(0, 255, 0);
	// stroke(255, 255, 0);
	// fill(0, 255, 255);
	// rect(50, 50, slider.value(), slider.value());
	// fill(255, 255, 255);
	// text(input.value(), 10, 10);

}
